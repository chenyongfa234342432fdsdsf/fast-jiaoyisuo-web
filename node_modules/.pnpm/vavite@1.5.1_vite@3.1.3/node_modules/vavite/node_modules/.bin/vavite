#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/Users/xiaoyu/Desktop/xiangmu/fastpay-web/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="$NODE_PATH:/Users/xiaoyu/Desktop/xiangmu/fastpay-web/node_modules/.pnpm/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../@vavite+multibuild-cli@1.5.1_vite@3.1.3/node_modules/@vavite/multibuild-cli/cli.js" "$@"
else
  exec node  "$basedir/../../../../../@vavite+multibuild-cli@1.5.1_vite@3.1.3/node_modules/@vavite/multibuild-cli/cli.js" "$@"
fi
